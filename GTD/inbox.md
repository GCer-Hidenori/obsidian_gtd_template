---
gtd: inbox
gtd_review_at: 2024-03-22
---

```tasks
(folder does not include archives) AND (folder does not include （没）) AND (not done) AND (tag does not include waiting) AND (tag does not include nextaction) AND (tag does not include someday) AND (tag does not include issue) AND (no scheduled date)
```
