---
gtd: calendar
gtd_review_at:
---

## 今日のカレンダー
```tasks
(folder does not include archives) AND (folder does not include （没）) AND (not done) AND (has scheduled date) AND (tag does not include waiting)  AND (tag does not include nextaction) AND (scheduled before or on today)

sort by scheduled
```

## 全体のカレンダー


```tasks
(folder does not include archives) AND (folder does not include （没）) AND (not done) AND (has scheduled date) AND (tag does not include waiting)  AND (tag does not include nextaction) 

sort by scheduled
```

